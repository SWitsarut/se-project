import React from "react";

export default async function page() {
  // const chatHistory = await fetch("http:/localhost:8080");
  const chatHistory = [{ admin: "suea" }, { admin: "poon" }, { admin: "fuse" }];
  return <div>page</div>;
}
